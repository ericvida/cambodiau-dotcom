{
    "version": 2,
    "buildCommand": "npm install --force && npm run build",
    "outputDirectory": "dist",
    "routes": [
        { 
            "src": "/[^.]+", 
            "dest": "/",
            "status": 200 
        }
    ]
}