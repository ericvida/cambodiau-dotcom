{
    "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }],
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "nodeVersion": "18.x"
            }
        }
    ]
}